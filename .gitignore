*.local.md
__pycache__/
*.pyc
.wrangler/
node_modules/

# Generated debug models written by the file adapter's baseDirectory (2.1MB, 25 files
# regenerated on every run). They also embed partially-masked object-store credentials,
# so there is no reason for them to be in history.
.aperio/

# Local Claude Code settings — 49 absolute /home/kstott paths, so machine-specific.
.claude/
